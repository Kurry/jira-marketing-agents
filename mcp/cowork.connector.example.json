{
  "comment": "Example Claude Cowork MCP connector entry for the AI Growth Ops server. Add this under your Cowork MCP/connector configuration (Settings → Connectors → Add custom MCP server), adjusting the absolute path and credentials. Cowork launches the server over stdio with this command.",
  "mcpServers": {
    "aigo-growth-ops": {
      "command": "npx",
      "args": ["tsx", "/absolute/path/to/jira-marketing-agents/mcp/server.ts"],
      "env": {
        "JIRA_BASE_URL": "https://your-site.atlassian.net",
        "JIRA_EMAIL": "you@example.com",
        "JIRA_API_TOKEN": "<atlassian-api-token>",
        "AIGO_PROJECT_KEY": "AIGO",
        "AIGO_ALLOW_WRITES": "false"
      }
    }
  }
}
